# 学習記録簿 要件定義・設計まとめ（v2.6.0）

## なぜこれが必要か

自分が「何を」・「どれだけ」やったのか可視化するため
やったことに対しどのくらい身についているのかを再確認するため

今までスプシでガントチャート作成とNotionで可視化していたが、見づらさを感じたため

将来的にはAIを入れて、これから取り組むことにどのくらいの時間(期間)がかかるかを予測できるようにしていきたい

## 概要

タスク（チケット）単位で学習を管理し、進捗・学習時間を可視化するWebアプリケーション。
タスクは **親タスク（グループ）** / **子タスク（実作業）** / **孫タスク（ステップ）** の3階層で管理する。
フロントエンドはHTML/CSS/バニラJS、バックエンドはNode.js（Express）、データベースはMySQLで構成するフルスタック構成。

---

## 機能要件

### 画面構成

| 画面               | 概要                                                                                                |
| ------------------ | --------------------------------------------------------------------------------------------------- |
| ① ダッシュボード   | 学習状況のサマリーとグラフを表示                                                                    |
| ② タスク一覧       | 親タスクをカンバン・テーブル・カレンダー・タイムラインの4ビューで表示                               |
| ③ タスク登録・編集 | 親タスク・子タスク・孫タスク（ステップ）の新規登録・編集・削除（モーダル）                          |
| ④ 書籍マスタ       | 書籍の一覧・登録・編集                                                                              |
| ⑤ マンダラチャート | 9×9マスの目標構造化ツール。複数チャートをDB保存。セル達成トグル・進捗バー・子タスク紐づけ機能を提供 |

---

### ① ダッシュボード画面

#### サマリーカード（4枚）

| カード         | 表示内容                                                               | サブテキスト                    |
| -------------- | ---------------------------------------------------------------------- | ------------------------------- |
| 期間の学習時間 | 期間トグル（週/月/年）と連動した学習時間（時間単位・小数点1桁）        | 先週比 / 先月比 / 前年比 ±N時間 |
| 進捗率         | 全タスクベースの完了率（%）                                            | 完了 N / 全 N 件                |
| 完了タスク     | 期間内の完了タスク数                                                   | 全 N 件中                       |
| 読了書籍       | `completed_count >= total_chapters` かつ `total_chapters > 0` の書籍数 | 読了 / 全 N 冊                  |

- サマリーカードの「期間の学習時間」ラベルは期間トグルに連動して「今週の学習時間」/「今月の学習時間」/「今年の学習時間」と切り替わる
- 学習時間はすべて **時間単位**（分単位ではなく `minutesToHours()` で小数点1桁に変換）で表示する

#### グラフ — レイアウト構成（B案：セクション分割方式）

画面は「学習時間」セクションと「進捗」セクションに分割し、セクション見出し（`.dashboard-section-title`）を表示する。

**学習時間セクション（2カラム）**

| グラフ             | 種類       | 説明                                                                                                                                        |
| ------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 日別学習時間       | 縦棒グラフ | 期間内の日別（週→曜日別、月→日別、年→月別）学習時間。予定時間（薄青）と実績時間（インディゴグラデーション）の2データセット。単位は時間（h） |
| カテゴリ別学習時間 | 縦棒グラフ | `category_name` でグループ化し `study_time` を合計。期間切り替えと連動。単位は時間（h）                                                     |

**進捗セクション（3カラム `.charts-row--triple`）**

| グラフ           | 種類           | 説明                                                                                                                   |
| ---------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------- |
| ステータス別件数 | ドーナツグラフ | 期間内タスクの未着手/進行中/完了の件数                                                                                 |
| 書籍別進捗率     | 横棒グラフ     | タスク数が1件以上ある書籍のみ表示。`completed_count / task_count` を%表示（0〜100%）。件数に応じて高さを動的計算       |
| カテゴリ別進捗率 | 横棒グラフ     | タスク数が1件以上あるカテゴリのみ表示。`category_name` ごとの完了数/総数を%表示（0〜100%）。件数に応じて高さを動的計算 |

**進捗率推移（全幅）**

| グラフ     | 種類         | 説明                                                  |
| ---------- | ------------ | ----------------------------------------------------- |
| 進捗率推移 | 折れ線グラフ | 予定進捗と実績進捗の2本の累積カーブ。凡例を下部に表示 |

- **予定進捗（%）**: `allTasks`（全期間）を対象に、各ラベル時点（その日付以前）の `end_planned_date` を持つタスク数 / 全タスク数
- **実績進捗（%）**: `allTasks`（全期間）を対象に、各ラベル時点以前の `end_date` を持ち `status === '完了'` のタスク数 / 全タスク数
- 期間種別ごとのラベル日付: 週→各曜日の23:59:59、月→各日の23:59:59、年→各月末の23:59:59

#### グラフのスタイル

- グラフの高さは動的計算を使用するもの（書籍別・カテゴリ別進捗率）とそれ以外で管理を分ける
  - 書籍別進捗率: `books-wrapper-js` のIDを持つ要素の height を `Math.max(件数 × barHeight, 250)px` で動的計算（件数が6超なら `barHeight=32`、以下なら `45`）
  - カテゴリ別進捗率: `category-wrapper-js` のIDを持つ要素の height を `Math.max(件数 × barHeight, 200)px` で動的計算（同上）
- グラフ軸のテキストカラーは `#aaa`（X軸ラベル）、`#eee`（Y軸ラベル横棒）
- グリッド線カラーは `rgba(255, 255, 255, 0.1)`
- `chartDaily` / `chartCategoryTime` は描画直後に `.resize()` でつぶれ防止する

#### 期間切り替え

- 週 / 月 / 年（一括切り替え）
  - 切り替えにより棒グラフの軸が変わる（週 → 曜日別、月 → 日別、年 → 月別）
  - サマリーカードも期間に連動して集計が変わる
- 前へ / 次へボタンで期間を移動（未来方向は現在期間まで）
- 期間ラベルで現在表示中の範囲を表示（例: "6/8 〜 6/14" / "2026年6月" / "2026年"）

#### カテゴリ別集計ロジック

- `filteredTasks`（現在の期間内のタスク）を `category_name` でグループ化
- `category_name` が `null` / 未定義の場合は「(言語不問)」として集計
- タスク数が0件のカテゴリ・書籍はグラフに表示しない
- 配色は `CHART_COLORS` 配列（8色）を書籍別進捗率と共通で使用し、カテゴリ数が多い場合は循環

#### Chart.jsインスタンス管理

- グラフ変数: `chartDaily`, `chartStatus`, `chartBooks`, `chartProgress`, `chartCategoryTime`, `chartCategoryProgress`
- `renderCharts()` の冒頭で全インスタンスを `.destroy()` してから再生成（重複エラー防止）

※ 件数・学習時間の集計はすべて **子タスク（実作業）** を基準とする。

---

### ② タスク一覧画面

タスクページ上部のタブでビューを切り替える。

#### カンバンビュー（デフォルト）

- カンバン形式（未着手 / 進行中 / 完了の3列）
- カンバンに並ぶカードは「親タスク（グループ）」。親自身はステータスを持たず、
  配置される列は子タスクのステータスから自動算出（ロールアップ）する
  - 子がすべて **当年中に完了**（`end_date` の年が現在年） → 完了列
  - 子がすべて未着手 → 未着手列（子が0件の親もここ）
  - それ以外（進行中あり／ステータス混在／過去年に完了したタスクを含む）→ 進行中列
- 完了列の表示は **当年完了分のみ** にスコープする。過去年に完了したタスクを含む親グループは進行中列に表示される
- 親カードはアコーディオン形式で開閉でき、開くと子タスク（実際の作業）の一覧を表示
  - 子タスク行には種別バッジ・カテゴリバッジ（「(言語不問)」含む常時表示）を表示する
  - 子タスク行には「◯/◯ステップ完了」バッジを表示（ステップが1件以上ある場合）
- ボタン構成
  - 画面上部の「親タスク追加」ボタン → 親（グループ）登録モーダルを開く
  - 各親カード内の「子タスク追加」行 → 子タスク登録モーダルを開く（親タスクIDを自動セット）
- 親カードのクリックで親の編集モーダル、子の行のクリックで子の編集モーダルを開く

#### テーブルビュー

- 全グループを縦に並べ、各グループの子タスクをテーブル形式で表示
- グループ行（タイトル・件数・「+」ボタン・編集ボタン）+ 子タスク行（タイトル・種別/カテゴリバッジ・ステータス・予定日・完了日・学習時間・ステップ進捗バッジ）
- グループ行の右端に「+」ボタンを配置し、クリックで子タスク登録モーダルを開く（`btn-add-task-table`）
- 子タスク行クリックで編集モーダルを開く（モーダル内でステップを管理）
- グループ行のクリックで子タスク行を折りたたみ/展開する（アコーディオン）
  - 折りたたみ状態は `localStorage`（キー: `collapsedTableGroups`、値: 閉じているgroupIdの配列のJSON）で保持する
  - `renderTable()` 実行時に `localStorage` から復元し、閉じているグループの子タスク行を `display: none` に設定する
  - 削除済みの親タスクIDが `collapsedTableGroups` に残存していてもDOMに対応要素がなければ無視される（実害なし）

#### カレンダービュー

- 月カレンダー形式で子タスクを表示
- `start_planned_date`（開始予定日）が当該日のセルにチップ表示
- 1セルに表示するチップは最大3件。4件目以降は「+N件」とまとめて表示
- チップにはカテゴリバッジ（「(言語不問)」含む常時表示）・ステップ進捗バッジを表示（ステップが1件以上ある場合）
- ← → ボタンで月を移動
- ヘッダー右端に「+ 子タスク追加」ボタン（`btn-add-task-calendar`）を配置する
  - クリックすると `openAddTaskGroupPicker()` でグループを選択して子タスク登録モーダルを開く
- チップクリックで編集モーダルを開く（モーダル内でステップを管理）
- 日付の比較はDateオブジェクトのローカルタイムゾーン（年・月・日）基準で行う

#### タイムラインビュー

- 月単位の横軸ガントチャート形式で子タスクを表示
- 左側にグループ名（140px固定幅）、右側に日付軸とバーを表示する2カラムグリッド構成
- 1行目は日付軸専用の行（1日〜月末までの日付を表示）
- `start_planned_date` 〜 `end_planned_date` の期間をバーで表示
  - 一方のみ設定されている場合、もう一方の値で補い1日分のバーとして表示
  - `end_planned_date` が `start_planned_date` より前の場合は同日として扱う
  - 期間が表示月の範囲外にかかる場合、表示月の範囲にクリップして表示
  - 両方未設定の場合はバーを表示しない（グループ行自体は表示する）
- グループ単位で行をまとめ、子タスク数に応じて行の高さを可変にする
- 本日の日付に縦の目印ライン（アンバー色）を表示する（表示中の月が今月の場合のみ）
- ← → ボタンで月を移動
- バークリックで編集モーダルを開く（モーダル内でステップを管理）
- 日付軸行の左側セル（グループ名列幅140pxと同じ）に月ナビゲーション（← →ボタン・年月ラベル）を配置する
- 年月ラベルをクリックすると年月ピッカー（`<input type="month">`）が開き、任意の年月に直接ジャンプできる
- ビューパネル上部のツールバー（`.timeline-toolbar`）に以下を配置する
  - 左端：「完了タスクを非表示」チェックボックス（`timeline-hide-completed`）
  - 右端：「+ 子タスク追加」ボタン（`btn-add-task-timeline`）
  - チェック時、`status === '完了'` の子タスクをフィルタし、バーおよび親タスク行ごと非表示
  - フィルタ後に子タスクが0件になった親グループ行は描画しない
  - **子タスクが最初から0件の親グループ**については、完了タスク非表示フィルタの対象外とし、グループ行として描画する（カンバンビューとの挙動統一）

#### 子タスク追加ボタンの動作（カレンダー・タイムライン共通）

- `openAddTaskGroupPicker(groups)` 関数を使用
  - グループが1件の場合: 直接その親タスクIDで子タスク登録モーダルを開く
  - グループが複数の場合: `prompt()` でグループ番号を入力させ、選択された親タスクIDでモーダルを開く

---

### ③ タスク登録・編集（モーダル）

- 親タスク用・子タスク用の2種類のモーダルを用意
- 親タスクモーダル：タイトル・メモ（・並び順）の登録/編集/削除（「未分類」は削除不可）
  - **子タスクが1件以上紐づいている親タスクは削除不可**（DBの外部キー制約 `ON DELETE RESTRICT` により弾かれる）
  - 削除失敗時はサーバーから返却されるエラーメッセージ（例:「このグループにはタスクが紐づいているため削除できません」）をalertで表示する
- 子タスクモーダル：タイトル・種別・書籍ID・ステータス・開始予定日・終了予定日・
  開始日・完了日・実績学習時間・予定学習時間・メモの登録/編集/削除
  - モーダル下部に**ステップ（孫タスク）セクション**を設ける
    - テキスト入力＋追加ボタンでステップを追加
    - 各ステップにチェックボックス（完了/未完了）と削除ボタン
    - ステップはモーダルを開いたタイミングでAPIから取得して表示
    - **ステップの追加・チェック・削除は即時API通信**で処理される（モーダルの「保存」を待たずに確定する）
    - そのため、ステップの変更はキャンセルや✕ボタンで閉じても元に戻らない（未保存確認ダイアログの対象外）
    - モーダルのDirty判定（`getTaskModalSnapshot()`）のスナップショットにステップ情報を含めているが、
      ステップのみを変更してキャンセルした場合でもダイアログが表示されることがある（既知の挙動・許容範囲内）
  - 「タスクを追加」から開いた場合は親タスクIDを自動セット
  - 単独で作る場合は親タスク＝「未分類」を初期選択
- いずれもタスク一覧画面上にモーダル表示し、操作はモーダル内で完結

#### 学習時間の単位変換ルール

- DB保存値は **分単位（整数）**
- モーダルの入力・表示は **時間単位（小数点1桁、h）**
- 保存時: `Math.round(parseFloat(入力値) * 60)` で分に変換
- 表示時: `minutesToHours(分) = Math.round((分 / 60) * 10) / 10` で時間に変換
- この変換は `tasks.js` の保存処理・表示処理・スナップショット取得で統一して適用する

#### モーダルのレイアウト

- 子タスクモーダルは画面の縦スクロールを抑えるため、フォーム項目を2カラムのグリッドで配置する
  - タイトル・紐づく書籍・メモは全幅（1カラム分）で表示
  - 種別/ステータス、カテゴリ/粒度、開始予定日/終了予定日、開始日/終了日、
    予定学習時間/学習時間はそれぞれ左右2列に並べる
- 親タスクモーダルはタイトル・メモのみのため、1カラムのまま

#### モーダルの開閉操作

- モーダル右上に ✕ ボタンを配置し、クリックでモーダルを閉じる
- モーダルの外側（オーバーレイ部分）をクリックした場合もモーダルを閉じる
- フッターのボタン構成は「削除」を左端、「キャンセル」「保存」を右側にまとめて配置する
- 編集中の内容に変更があり、保存せずにモーダルを閉じようとした場合、
  「編集の内容は保存されていません。閉じてもよろしいですか？」という確認ダイアログを表示する
  - OKの場合のみモーダルを閉じる。キャンセルした場合は編集状態を維持する
  - 「保存」「削除」が成功した直後は変更なしの状態として扱い、
    その後にモーダルを閉じる際は確認ダイアログを表示しない
- 上記の開閉操作・確認ダイアログは親タスクモーダル・子タスクモーダルの両方に適用する

---

### ④ 書籍マスタ画面

- Google Books API を使って書籍を検索・登録
- 登録済み書籍を「登録済みの書籍を絞り込む」入力（`#book-filter-input`）でタイトル・著者によりフィルタ表示できる
- 書籍カードには表紙画像・タイトル・著者・メモ・進捗（全何章の場合のみ）を表示する
  - 進捗は「進捗 N%（完了数 / 全何章）」の形式で表示する
  - 進捗率は、その書籍に紐づく子タスクのうち `ステータス = 完了` の件数を `全何章` で割って算出する
  - 進捗率は `Math.min(100, ...)` で100%を上限としてクリップする
    （1章に複数タスクを紐づけた場合に完了数が章数を超えても表示上は100%止まり）
  - `全何章` が未設定の書籍は進捗バー・進捗率を表示しない
- 書籍編集モーダルで「全何章」「メモ」を編集できる

---

### ⑤ 設定画面

#### タスク種別管理

- 子タスクの「種別」項目をユーザーが自由に追加・編集・削除できる
- 種別一覧をリスト表示し、各行に「編集」「削除」ボタンを配置
  - 編集はインライン編集方式（クリックでテキスト入力に切り替え、保存/キャンセル）
- 下部のテキスト入力＋追加ボタンで新規種別を追加
- 使用中（子タスクに紐づいている）の種別は削除不可。削除しようとした場合、
  使用件数を含むエラーメッセージを表示する

#### カテゴリ（言語/技術）管理

- 子タスクの「カテゴリ」項目をユーザーが自由に追加・編集・削除できる
- 「タスク種別管理」と同一画面・同一UIパターンで、カテゴリ一覧をリスト表示し、各行に「編集」「削除」ボタンを配置
  - 編集はインライン編集方式
- 下部のテキスト入力＋追加ボタンで新規カテゴリを追加
- 使用中（子タスクに紐づいている）のカテゴリは削除不可
- 「(言語不問)」（id=1）は編集・削除不可

---

### ⑥ マンダラチャート画面

#### 概要

9×9マスのマンダラチャートを使って目標を構造化するツール。複数のマンダラチャートをDBに保存し、一覧から切り替えて表示できる。各セルに既存の子タスクを複数紐づけることができ、タスクのステータスをセル上で確認できる。

#### ナビゲーション

- サイドバーのメインナビに「マンダラチャート」リンクを追加する（Lucideアイコン: `grid-3x3`）
- ルーティングは `#page-mandala`。`mandalaInitialized` フラグで初回のみ `initMandala()` を実行する

#### レイアウト

- 画面上部：新規作成フォーム（タイトル入力＋作成ボタン）
- 画面中部：作成済みマンダラの一覧リスト（タイトル・最終更新日・削除ボタン）
- 画面下部：選択中のマンダラチャート本体（3×3ブロック構成の9×9グリッド）

#### グリッド構造（3×3ブロック × 3×3セル）

```
[ブロック0,0][ブロック0,1][ブロック0,2]
[ブロック1,0][ブロック1,1（中心ブロック）][ブロック1,2]
[ブロック2,0][ブロック2,1][ブロック2,2]
```

各ブロックは3×3の9セルで構成。グリッド全体は9×9=81セル。

| セルタイプ | 座標                                                                                | 個数 | 役割                 |
| ---------- | ----------------------------------------------------------------------------------- | ---- | -------------------- |
| 中心テーマ | `(4,4)`（中心ブロックの中心）                                                       | 1    | 達成したい大テーマ   |
| サブテーマ | 各3×3ブロックの中心 `(1,1)` `(1,4)` `(1,7)` `(4,1)` `(4,7)` `(7,1)` `(7,4)` `(7,7)` | 8    | 大テーマを支える要素 |
| アクション | 上記以外の全セル                                                                    | 72   | 具体的なアクション   |

> `getCellType(r, c)` 関数で `r === 4 && c === 4` → `"center"`、`r % 3 === 1 && c % 3 === 1` → `"sub-center"`、それ以外 → `"action"` と判定する。

#### セルの見た目

| セルタイプ   | 背景                                                   | 文字色             |
| ------------ | ------------------------------------------------------ | ------------------ |
| 中心テーマ   | インディゴ→アンバーのグラデーション                    | プライマリ・太字   |
| サブテーマ   | `rgb(99 102 241 / 0.2)`（インディゴ薄め）              | `#a5b4fc`・太字    |
| アクション   | `var(--color-bg-card)`                                 | プライマリ         |
| 達成済みセル | `rgb(52 211 153 / 0.12)`（緑系）に変化（全タイプ共通） | `hsl(152 60% 70%)` |

#### セル入力

- 各セルはテキストエリア（`<textarea>`）で自由記述
- Tabキーで左→右→次行の順に次セルへフォーカス移動（`Shift+Tab` で逆順）
- 入力後1秒で自動保存（デバウンス）。保存状態を右上のステータス表示（「保存中…」「✓ 保存済み」）で通知

#### 達成トグル機能

- **アクションセル・サブテーマセル**の右上に小さな達成ボタン（○アイコン）を配置する
  - 通常時は非表示（`.mandala-cell:hover` でのみ表示）
  - 達成済みの場合は常時表示（緑色チェックマーク）
- ボタンをクリックすると達成/未達成をトグルする
  - 達成時: セル背景が緑系に変化、アイコンがチェックマークに変化
  - 未達成に戻す: 元の背景色・アイコンに戻る
- 達成状態は **即時API通信**で `is_completed` フィールドとしてDBに保存する（テキスト自動保存とは独立）
- 中心テーマセル `(4,4)` には達成ボタンを配置しない

#### 進捗バー

- チャート上部に「達成 N / 80（N%）」の進捗バーを表示する
  - 対象は中心テーマセル `(4,4)` を除いた80セル
- 達成トグル操作時に即時更新（APIレスポンスを待たずUIを先行更新）

#### セルへの子タスク紐づけ機能（v2.6.0追加）

- **1つのセルに複数の子タスクを紐づけることができる**
- 紐づけた子タスクのタイトルをセル内に一覧表示する（テキストエリアの下部に小さく表示）
- タスクのステータスバッジ（未着手・進行中・完了）をタイトル横に表示する

**紐づけ操作フロー：**

1. セル内の「＋タスク」ボタン（ホバー時に表示）をクリック
2. 子タスク選択モーダルが開く
   - 全子タスクを親タスク（グループ）ごとにグループ化して一覧表示
   - 既に紐づき済みのタスクはチェック済み状態で表示
   - タイトルでインクリメンタル検索できる
   - チェックボックスで複数選択し「保存」で確定（即時API通信）
3. モーダルを閉じるとセルのタスク一覧が更新される

**紐づけ解除：**

- セル内のタスク表示エリアの各タスク横に「×」ボタンを配置し、クリックで即時解除（即時API通信）

**タスク紐づけの表示：**

- セル内下部のタスクエリアに各タスクのタイトルを縦に並べる
- タイトル横にステータスドット（未着手:グレー / 進行中:インディゴ / 完了:グリーン）を表示
- タスクタイトルをクリックすると子タスク編集モーダルを開く（タスク一覧画面の `openTaskEditModal()` を流用）

#### 一覧・CRUD操作

- **新規作成**: タイトルを入力して「+ 新規作成」ボタン（またはEnterキー）で作成。9×9=81セルを空の状態でDBに一括挿入し、作成後そのマンダラを自動選択して表示する
- **選択**: 一覧のアイテムをクリックしてチャートを切り替える。未保存のテキスト変更がある場合は切り替え前に即時保存する
- **削除**: 一覧の「削除」ボタンをクリック。確認ダイアログ後にマンダラ本体・全セル・全紐づきを削除する（`ON DELETE CASCADE`）
- **タイトル更新**: 現バージョンでは一覧表示のみ（タイトル編集UIは未実装）

#### 凡例

チャート下部に色分けの凡例を表示する（中心テーマ / サブテーマ / アクション / 達成済み）。

---

## データ構造

### 親タスク（グループ）

| 項目       | 型     | 必須 | 備考                                    |
| ---------- | ------ | ---- | --------------------------------------- |
| 親タスクID | number | ✅   | 自動採番（INT UNSIGNED）                |
| タイトル   | string | ✅   | まとめ用の名前。例：「Reactを理解する」 |
| メモ       | string | ー   | 自由記述                                |
| 並び順     | number | ー   | カンバン内の表示順（任意）              |

> ※ 親はステータス・種別・学習時間を持たない。ステータスは子から自動算出。
> 「未分類」は削除不可（id=1固定）。
> 子タスクが1件以上紐づいている親タスクは削除不可（`ON DELETE RESTRICT`）。
> 削除失敗時はAPIエラーレスポンスをフロントでalert表示する。

### 子タスク（実作業）

| 項目         | 型     | 必須 | 備考                                                                              |
| ------------ | ------ | ---- | --------------------------------------------------------------------------------- |
| 子タスクID   | number | ✅   | 自動採番（INT UNSIGNED）                                                          |
| 親タスクID   | number | ✅   | どの親に属するか（`groups.id` 参照・外部キー）                                    |
| タイトル     | string | ✅   | 例：「useStateを理解する」                                                        |
| 種別         | number | ✅   | `task_types.id` 参照（外部キー）                                                  |
| カテゴリ     | number | ー   | `task_categories.id` 参照。未設定時は「(言語不問)」（id=1）                       |
| 粒度         | enum   | ー   | 章単位 / トピック単位                                                             |
| 紐づく書籍ID | number | ー   | `books.id` 参照（外部キー）。種別が「本」の場合に選択                             |
| ステータス   | enum   | ✅   | 未着手 / 進行中 / 完了                                                            |
| 開始予定日   | date   | ー   | タイムライン・カレンダーの表示に使用                                              |
| 終了予定日   | date   | ー   | タイムラインのバー終端に使用                                                      |
| 開始日       | date   | ー   | 実際の作業開始日                                                                  |
| 完了日       | date   | ー   | 実際の完了日。ダッシュボードの集計基準・カンバン完了列の年判定基準                |
| 実績学習時間 | number | ー   | **DB保存値は分単位（整数）**。モーダルでは時間単位（h）で入出力し保存時に分へ変換 |
| 予定学習時間 | number | ー   | 同上                                                                              |
| メモ         | string | ー   | 自由記述                                                                          |

### 孫タスク（ステップ）

| 項目       | 型      | 必須 | 備考                                                                         |
| ---------- | ------- | ---- | ---------------------------------------------------------------------------- |
| ステップID | number  | ✅   | 自動採番（INT UNSIGNED）                                                     |
| 子タスクID | number  | ✅   | `tasks.id` 参照（外部キー）。子タスク削除時は `ON DELETE CASCADE` で自動削除 |
| タイトル   | string  | ✅   | 例：「公式ドキュメントのp.30〜50を読む」                                     |
| 完了フラグ | boolean | ✅   | false: 未完了 / true: 完了（デフォルト: false）                              |
| 並び順     | number  | ー   | モーダル内の表示順（デフォルト: 0）                                          |

> ※ ステップの追加・チェック・削除は即時API通信で処理され、モーダルの「保存」を待たずに確定する。
> キャンセルや✕ボタンで閉じても、ステップへの変更は元に戻らない。

### 書籍マスタ

| 項目          | 型     | 必須 | 備考                                                 |
| ------------- | ------ | ---- | ---------------------------------------------------- |
| 書籍ID        | number | ✅   | 自動採番（INT UNSIGNED）                             |
| タイトル      | string | ✅   | Google Books APIから取得                             |
| 著者          | string | ✅   | Google Books APIから取得                             |
| カバー画像URL | string | ー   | Google Books APIから取得                             |
| メモ          | string | ー   | 自由記述                                             |
| 全何章        | number | ー   | `total_chapters`。書籍編集モーダルで入力（未設定可） |

> ※ 進捗（完了数 / 全何章）は保存せず、`GET /api/books` で都度算出して返す
> （`completed_count`：その書籍に紐づく子タスクのうち `ステータス = 完了` の件数）。
> `completed_count` はSQLの `SUM()` により文字列で返る場合があるため、
> フロント側で `Number()` に変換してから計算する。
> 進捗率は `Math.min(100, ...)` で100%を上限にクリップする。

### タスク種別マスタ

| 項目   | 型     | 必須 | 備考                                |
| ------ | ------ | ---- | ----------------------------------- |
| 種別ID | number | ✅   | 自動採番（INT UNSIGNED）            |
| 種別名 | string | ✅   | 一意。例：「本」「動画」「YouTube」 |
| 並び順 | number | ー   | 一覧表示順（デフォルト: 0）         |

### タスク言語/カテゴリマスタ

| 項目       | 型     | 必須 | 備考                                                     |
| ---------- | ------ | ---- | -------------------------------------------------------- |
| カテゴリID | number | ✅   | 自動採番（INT UNSIGNED）                                 |
| カテゴリ名 | string | ✅   | 一意。例：「Java」「Python」「JavaScript」「(言語不問)」 |
| 並び順     | number | ー   | 一覧表示順（デフォルト: 0）                              |

### マンダラチャート

**mandalas テーブル**

| 項目         | 型       | 必須 | 備考                     |
| ------------ | -------- | ---- | ------------------------ |
| マンダラID   | number   | ✅   | 自動採番（INT UNSIGNED） |
| タイトル     | string   | ✅   | 例：「2026年の目標」     |
| 作成日時     | datetime | ✅   | 自動設定                 |
| 最終更新日時 | datetime | ✅   | 自動更新                 |

**mandala_cells テーブル**

| 項目           | 型      | 必須 | 備考                                                                 |
| -------------- | ------- | ---- | -------------------------------------------------------------------- |
| セルID         | number  | ✅   | 自動採番（INT UNSIGNED）                                             |
| マンダラID     | number  | ✅   | `mandalas.id` 参照（外部キー）。マンダラ削除時は `ON DELETE CASCADE` |
| 行インデックス | number  | ✅   | 0〜8（`TINYINT UNSIGNED`）                                           |
| 列インデックス | number  | ✅   | 0〜8（`TINYINT UNSIGNED`）                                           |
| テキスト内容   | string  | ー   | セルのテキスト（`TEXT`）                                             |
| 達成フラグ     | boolean | ✅   | `is_completed`。0: 未達成 / 1: 達成（デフォルト: 0）                 |

> ※ `(mandala_id, row_index, col_index)` にUNIQUE制約を設ける。
> マンダラ新規作成時に81セル（9×9）を空の状態で一括INSERTする。

**mandala_cell_tasks テーブル（v2.6.0追加）**

| 項目       | 型     | 必須 | 備考                                                                  |
| ---------- | ------ | ---- | --------------------------------------------------------------------- |
| ID         | number | ✅   | 自動採番（INT UNSIGNED）                                              |
| セルID     | number | ✅   | `mandala_cells.id` 参照（外部キー）。セル削除時は `ON DELETE CASCADE` |
| 子タスクID | number | ✅   | `tasks.id` 参照（外部キー）。タスク削除時は `ON DELETE CASCADE`       |

> ※ `(cell_id, task_id)` にUNIQUE制約を設け、同じセルへの同一タスクの重複紐づけを防ぐ。
> 1セルに複数タスクを紐づけ可能。1タスクを複数セルに紐づけることも可能（多対多の関係）。
> セル削除・タスク削除のいずれでも紐づきレコードは `ON DELETE CASCADE` で自動削除される。

---

## 外部API

| API              | 用途                                               |
| ---------------- | -------------------------------------------------- |
| Google Books API | 書籍情報の検索・取得（タイトル・著者・カバー画像） |

---

## 非機能要件

| 項目             | 内容                                                           |
| ---------------- | -------------------------------------------------------------- |
| 対応ブラウザ     | Chrome・Firefox・Safari（スマホブラウザ含む）                  |
| レスポンシブ     | スマホ・タブレット・PC対応（モバイルは下部タブバー表示）       |
| パフォーマンス   | タスク数百件以上を想定                                         |
| セキュリティ     | フェーズ1はログイン不要（個人利用）・APIはローカル環境のみ想定 |
| アクセシビリティ | 最低限対応（ボタンへのラベル付与など）                         |
| テーマ           | ライト/ダークモード切り替え対応（localStorageで保持）          |

---

## 技術設計

### 技術選定

| 項目               | 選定                              |
| ------------------ | --------------------------------- |
| フロントエンド言語 | HTML / CSS / JavaScript（バニラ） |
| バックエンド       | Node.js + Express                 |
| データベース       | MySQL（phpMyAdminで管理）         |
| グラフ             | Chart.js（CDN）                   |
| フォント           | Google Fonts（CDN）               |
| CSSフレームワーク  | なし（素のCSS・CSS変数）          |
| ビルドツール       | 不要                              |
| データ永続化       | MySQL（REST API経由）             |

### システム構成

```
フロントエンド (HTML/CSS/JS)
        ↕ fetch() / REST API
バックエンド (Node.js + Express)
        ↕ mysql2
データベース (MySQL / phpMyAdmin)
```

### APIエンドポイント一覧

| メソッド | パス                                        | 内容                                                        |
| -------- | ------------------------------------------- | ----------------------------------------------------------- |
| GET      | `/api/groups`                               | 親タスク一覧取得                                            |
| POST     | `/api/groups`                               | 親タスク新規作成                                            |
| PUT      | `/api/groups/:id`                           | 親タスク更新                                                |
| DELETE   | `/api/groups/:id`                           | 親タスク削除（「未分類」・子タスク有りは不可）              |
| GET      | `/api/tasks`                                | 子タスク一覧取得（book_titleをJOINして返す）                |
| GET      | `/api/tasks/:id`                            | 子タスク1件取得                                             |
| POST     | `/api/tasks`                                | 子タスク新規作成                                            |
| PUT      | `/api/tasks/:id`                            | 子タスク更新                                                |
| DELETE   | `/api/tasks/:id`                            | 子タスク削除                                                |
| GET      | `/api/books`                                | 書籍一覧取得                                                |
| POST     | `/api/books`                                | 書籍登録                                                    |
| PUT      | `/api/books/:id`                            | 書籍更新                                                    |
| DELETE   | `/api/books/:id`                            | 書籍削除                                                    |
| GET      | `/api/tasks/:id/steps`                      | ステップ一覧取得（子タスクに紐づく）                        |
| POST     | `/api/tasks/:id/steps`                      | ステップ新規作成                                            |
| GET      | `/api/steps`                                | 全ステップ一覧取得（カンバン等の進捗バッジ表示用）          |
| PUT      | `/api/steps/:id`                            | ステップ更新（タイトル・完了フラグ）                        |
| DELETE   | `/api/steps/:id`                            | ステップ削除                                                |
| GET      | `/api/task-types`                           | タスク種別一覧取得                                          |
| POST     | `/api/task-types`                           | タスク種別新規作成                                          |
| PUT      | `/api/task-types/:id`                       | タスク種別更新                                              |
| DELETE   | `/api/task-types/:id`                       | タスク種別削除（使用中は不可）                              |
| GET      | `/api/task-categories`                      | カテゴリ一覧取得                                            |
| POST     | `/api/task-categories`                      | カテゴリ新規作成                                            |
| PUT      | `/api/task-categories/:id`                  | カテゴリ更新（「(言語不問)」は不可）                        |
| DELETE   | `/api/task-categories/:id`                  | カテゴリ削除（使用中・「(言語不問)」は不可）                |
| GET      | `/api/mandalas`                             | マンダラ一覧取得（updated_at降順）                          |
| GET      | `/api/mandalas/:id`                         | マンダラ1件取得（cells配列・各セルのtasks配列含む）         |
| POST     | `/api/mandalas`                             | マンダラ新規作成（81セルを同時INSERT）                      |
| PUT      | `/api/mandalas/:id`                         | マンダラタイトル更新                                        |
| DELETE   | `/api/mandalas/:id`                         | マンダラ削除（セル・紐づきはCASCADE）                       |
| PUT      | `/api/mandalas/:id/cells`                   | セル一括更新（`content` / `is_completed` を部分的に更新可） |
| GET      | `/api/mandalas/cells/:cellId/tasks`         | セルに紐づく子タスク一覧取得                                |
| POST     | `/api/mandalas/cells/:cellId/tasks`         | セルに子タスクを紐づける（`{ task_id }` を受け取る）        |
| DELETE   | `/api/mandalas/cells/:cellId/tasks/:taskId` | セルからタスクの紐づきを解除                                |

### ルーティング

`location.hash` 方式を採用。

- URLで状態管理できる
- ブラウザの戻るボタンが使える
- サイドバーのリンクは `<a href="#page-xxx">` の形式
- 各ページは初回表示時のみ初期化（`booksInitialized` / `dashboardInitialized` / `settingsInitialized` / `mandalaInitialized` フラグで管理）
  - 各 `initXxx()` 内で `document.getElementById(...).addEventListener(...)` のように
    要素取得直後に `addEventListener` を呼ぶ箇所がある場合、対応するHTML要素が
    `index.html` に存在しないと `showPage()` 全体が例外で中断し、
    以後すべてのページ遷移ができなくなる（特定ページでリロード→他ページへ遷移できない不具合の典型例）
  - フラグ変数名は宣言時の名称と設定時の名称を一致させること
  - `initXxx()` 内で要素を取得してイベントを登録する際は、要素の存在を確認してから処理するか、
    対応する要素が `index.html` に存在することを必ず確認すること

### データ型の統一

- 親タスク・子タスク・書籍・マスタテーブルのIDはすべて `INT UNSIGNED`（number）で統一する
- 日付フィールド（`start_planned_date`、`end_planned_date`、`start_date`、`end_date`）は
  DBの型を `DATE`（YYYY-MM-DD）とする

### ライト/ダークモード

- サイドバー下部のトグルボタンで切り替え
- `localStorage` に保存し、次回アクセス時も復元
- テーマクラス（`light`）は `<html>` 要素に付与する（`document.documentElement.classList`）
  - CSSセレクタは `html.light { ... }` とする
- CSSにハードコードの色値は使用せず、必ず CSS変数（`var(--color-bg-sidebar)` 等）で指定する

### モーダル

HTMLに事前記述・JSで表示/非表示する方式を採用。親タスク用・子タスク用・書籍用の3種類を用意する。

- `.modal` には `z-index` を明示的に指定する（タイムラインのバー等より手前に表示するため）
- `.modal.hidden { display: none; }` でオーバーレイの `display: flex` を上書き

#### ステップの即時保存と未保存確認ダイアログの関係

- ステップ（孫タスク）の操作（追加・チェック・削除）は即時API通信で確定する
- モーダルの「保存」「キャンセル」「✕」ボタンはステップへの変更に影響しない

#### モーダルの開閉・未保存確認

- `markXxxModalClean()`：モーダルを開いた直後・保存・削除が成功した直後に呼び、
  現在のフォーム値を `dataset.initialSnapshot` にJSON文字列として保存する
- `isXxxModalDirty()`：現在のフォーム値のスナップショットと `initialSnapshot` を比較する
- `closeXxxModalWithConfirm()`：`isXxxModalDirty()` が true の場合のみ確認ダイアログを表示

### カンバンの描画方針

- 「データから盤面をまるごと作り直す」描画関数を1本用意する
- 親の所属列は保存せず、毎回子のステータス配列から算出する
- カンバン全体の再描画時、開いているアコーディオンの開閉状態を再描画前に記録し再描画後に復元する

#### `calcStatus` の完了判定ロジック（v2.3.0〜）

```javascript
function calcStatus(groupId, tasks) {
  const children = tasks.filter((t) => t.group_id === groupId);
  if (children.length === 0) return "未着手";

  const currentYear = new Date().getFullYear();
  const completedThisYear = (t) =>
    t.status === "完了" &&
    t.end_date &&
    new Date(t.end_date).getFullYear() === currentYear;

  if (children.every(completedThisYear)) return "完了";
  if (children.every((t) => t.status === "未着手")) return "未着手";
  return "進行中";
}
```

### タスクビューの状態管理

- `currentView` 変数でアクティブなビューを管理（`'kanban'` / `'table'` / `'calendar'` / `'timeline'`）
- カレンダー・タイムラインの表示月はそれぞれ `calendarDate`・`timelineDate`（Dateオブジェクト、日付は1日固定）で管理
- `fetchTaskViewData()` で `groups` / `tasks` / `books` / `steps` を一括取得し、各renderに共通で渡す
- 選択中のビューは `localStorage`（キー: `taskView`）に保存し、リロード後も復元する

### マンダラチャートの状態管理

- `mandalas`：一覧データ（配列）
- `currentMandala`：現在表示中のマンダラオブジェクト（cells配列・各セルのtasks配列含む）
- `pendingCells`：未保存のテキスト変更 `{ "r,c": { content } }` のキーバリューマップ
- `saveTimer`：自動保存用のデバウンスタイマーID

#### セルのテキスト自動保存フロー

1. `textarea` の `input` イベントで `pendingCells` にデータを蓄積
2. 1秒後に `autoSave()` を実行（デバウンス）
3. `PUT /api/mandalas/:id/cells` に `{ cells: [{ row_index, col_index, content }] }` を送信
4. 保存ステータスを「保存中…」→「✓ 保存済み」と更新

#### セルの達成トグルフロー

1. `.mandala-complete-btn` クリックで UI を即時更新（クラス付け替え・アイコン変更・進捗バー更新）
2. `PUT /api/mandalas/:id/cells` に `{ cells: [{ row_index, col_index, is_completed: 0 or 1 }] }` を送信
3. テキスト自動保存とは独立して即時保存（デバウンスなし）

#### セルへのタスク紐づけフロー（v2.6.0追加）

1. セルの「＋タスク」ボタンクリックで子タスク選択モーダルを開く
2. `GET /api/tasks` で全タスクを取得し、親タスクグループ別に一覧表示
3. `GET /api/mandalas/cells/:cellId/tasks` で現在の紐づき済みタスクIDを取得し、チェック状態を初期化
4. ユーザーがチェックボックスで選択/解除し「保存」をクリック
5. 追加分は `POST /api/mandalas/cells/:cellId/tasks`、解除分は `DELETE /api/mandalas/cells/:cellId/tasks/:taskId` を呼び出す
6. モーダルを閉じ、セルのタスク表示エリアを再描画する

#### マンダラ切り替え時の保存保証

- `loadMandala(id)` 呼び出し時に `pendingCells` が空でなければ `clearTimeout` + `autoSave()` を先行実行してから切り替える

#### マンダラAPIの拡張（v2.6.0追加）

- `GET /api/mandalas/:id` のレスポンスの各セルオブジェクトに `tasks` 配列（紐づく子タスクの `id`, `title`, `status`）を含める
  - SQLは `mandala_cells LEFT JOIN mandala_cell_tasks LEFT JOIN tasks` でJOINして取得
- `GET /api/mandalas/cells/:cellId/tasks` はセル単体の紐づきタスク一覧を返す（再取得用）

### タイムラインビューの月ナビゲーション

- 月ナビゲーション（← →ボタン・年月ラベル・年月ピッカー）は日付軸専用行の左側セル（140px幅）に内包する
- `renderTimeline()` 実行時にDOMを毎回再生成するため、イベントリスナーは `bindTimelineEvents()` で再バインドする

### カレンダー・タイムラインからの子タスク追加

- カレンダーヘッダー右端に `btn-add-task-calendar` を配置する
- タイムラインツールバー右端に `btn-add-task-timeline` を配置する
- 両ボタンのイベントは `bindAddTaskButtons(groups)` 内で `onclick` として設定する

### デザイン方針

| 項目               | 内容                                                                                                          |
| ------------------ | ------------------------------------------------------------------------------------------------------------- |
| テーマ             | Refined Dark Theme（デフォルト）／ライトモード切り替え対応                                                    |
| アクセントカラー   | インディゴ〜アンバー系                                                                                        |
| 見出しフォント     | Syne                                                                                                          |
| 本文フォント       | DM Sans                                                                                                       |
| アイコン           | Lucide（CDN: `https://unpkg.com/lucide@latest`）- `data-lucide` 属性で指定し、`lucide.createIcons()` で初期化 |
| モバイルレイアウト | 画面下部に固定タブバー（ダッシュボード・タスク・書籍）                                                        |

### サイドバー構造

- `<nav>` の `padding` は上下 `var(--space-32)`、左右 `var(--space-24)`
- ロゴボックス（`.nav-logo-box`）は `44px × 44px`、`border-radius: 13px`、フォントサイズ `22px`
- `.nav-footer` は `margin-top: auto` でサイドバー下部に固定
- テーマトグルボタン（`#btn-theme-toggle`）は `width: auto`（全幅にしない）
- 設定リンクは `.nav-footer` 内に `<a>` タグとして配置する

---

## ファイル構成

```
project/
├── server/
│   ├── server.js          # Expressサーバー本体・ミドルウェア設定
│   ├── db.js              # MySQL接続設定（MAMP port:8889）
│   ├── routes/
│   │   ├── groups.js      # 親タスク（グループ）CRUD API
│   │   ├── tasks.js       # 子タスクCRUD API（books LEFT JOIN含む）
│   │   ├── steps.js       # ステップ（孫タスク）CRUD API
│   │   ├── books.js       # 書籍マスタ API
│   │   ├── task_types.js  # タスク種別マスタ CRUD API（使用中削除ガード）
│   │   ├── task_categories.js  # タスク言語/カテゴリマスタ CRUD API（使用中・(言語不問)削除ガード）
│   │   └── mandala.js     # マンダラチャート CRUD API（セル一括更新・タスク紐づけ含む）
│   └── schema.sql         # DBテーブル定義・初期データ
├── public/                # Expressが静的配信するフロントエンド
│   ├── index.html
│   ├── css/
│   │   ├── style.css      # 共通・CSS変数・レイアウト・ライト/ダークテーマ・レスポンシブ
│   │   ├── dashboard.css  # ダッシュボード固有・期間タブ
│   │   ├── tasks.css      # タスク一覧・カンバン・テーブル・カレンダー・タイムライン・タブUI
│   │   ├── books.css      # 書籍マスタ
│   │   ├── modal.css      # モーダル共通・ステップ（孫タスク）セクション
│   │   ├── settings.css   # 設定画面・タスク種別管理リスト
│   │   └── mandala.css    # マンダラチャート画面・グリッド・セル・達成トグル・タスク紐づけ・進捗バー
│   └── js/
│       ├── config.js      # Google Books APIキー（.gitignore対象）
│       ├── api.js         # fetch()ラッパー（REST API呼び出し共通処理）
│       ├── app.js         # ルーティング・初期化・ライト/ダークモード切り替え
│       ├── dashboard.js   # ダッシュボード描画・グラフ・期間切り替え
│       ├── tasks.js       # カンバン・テーブル・カレンダー・タイムライン・モーダル・種別/カテゴリ色分け
│       ├── books.js       # 書籍マスタ・Google Books API
│       ├── settings.js    # タスク種別・カテゴリ管理（追加・編集・削除）
│       └── mandala.js     # マンダラチャート描画・セル入力・達成トグル・タスク紐づけ・自動保存
└── package.json
```

---

## 改訂履歴

| 日付       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-17 | 初版作成（要件定義）                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-05-20 | 技術設計・ファイル構成を追記                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-05-23 | バックエンドをNode.js(Express)+MySQLに変更、ファイル構成・実装順序・APIエンドポイントを更新                                                                                                                                                                                                                                                                                                                                             |
| 2026-05-30 | 親子タスク構造を導入。カンバン仕様・データ構造・API・モーダル・ファイル構成・実装順序を更新                                                                                                                                                                                                                                                                                                                                             |
| 2026-06-10 | タスクビューにテーブル・カレンダー・タイムラインを追加。子タスクに日付4項目・予定学習時間を追加。ライト/ダークモード・レスポンシブ対応・ダッシュボード期間切り替えを追加                                                                                                                                                                                                                                                                |
| 2026-06-12 | ステップ（孫タスク）機能を追加。task_stepsテーブル・ステップAPI・子タスクモーダルへのステップUIを追加。各ビューにステップ進捗バッジを追加                                                                                                                                                                                                                                                                                               |
| 2026-06-13 | タスク一覧にビュー切り替えタブ（カンバン/テーブル/カレンダー/タイムライン）を追加し、テーブル・カレンダー・タイムラインビューを実装。GET /api/stepsを追加。カレンダー・タイムラインの日付計算ロジック、モーダルのz-index指定を明確化                                                                                                                                                                                                    |
| 2026-06-13 | タスク種別をENUMからマスタテーブル方式に変更。task_typesテーブル・タスク種別CRUD APIを追加し、設定画面で種別の追加・編集（インライン）・削除（使用中は削除不可）を可能に。カンバン/テーブルの種別バッジを種別名ベースの自動色分けに変更                                                                                                                                                                                                 |
| 2026-06-13 | 言語/技術カテゴリ軸を追加。task_categoriesテーブル・カテゴリCRUD APIを追加し、子タスクにcategory_idを追加（初期値「(言語不問)」）。設定画面に「カテゴリ管理」セクションを追加。各ビューにカテゴリバッジを追加                                                                                                                                                                                                                           |
| 2026-06-13 | 子タスクモーダルを2カラムグリッドレイアウトに変更し縦スクロールを抑制。親タスク・子タスクモーダルに✕ボタン・外側クリック・未保存確認ダイアログを追加。フッターのボタン配置を統一。カンバン再描画時のアコーディオン状態保持を追加                                                                                                                                                                                                        |
| 2026-06-14 | 書籍マスタに `total_chapters` を追加。書籍カードに進捗表示。書籍マスタ画面に絞り込み検索を追加                                                                                                                                                                                                                                                                                                                                          |
| 2026-06-14 | タイムラインビューの月ナビゲーションを日付軸行の左側セルに移動。年月ラベルクリックで年月ピッカーを開く機能を追加                                                                                                                                                                                                                                                                                                                        |
| 2026-06-14 | 不具合修正：`allBooks`変数名重複解消・`#book-filter-input`をindex.htmlに追加・`dashboardInitialized`誤字修正                                                                                                                                                                                                                                                                                                                            |
| 2026-06-14 | ダッシュボードにカテゴリ別学習時間・カテゴリ別進捗率グラフを追加。進捗率推移を累積カーブに変更。レイアウトをB案（セクション分割方式）に変更（v2.0.0）                                                                                                                                                                                                                                                                                   |
| 2026-06-14 | Lucideアイコンを導入。サイドバーのナビアイコンを絵文字からLucideアイコンに変更。設定リンクを `.nav-footer` 内の `<a>` タグに移動。サイドバーのpaddingおよびロゴサイズを調整（v2.1.0）                                                                                                                                                                                                                                                   |
| 2026-06-14 | 不具合修正：書籍別・カテゴリ別進捗率グラフの高さを動的計算。ライトモードのテーマクラスを `html.light` に変更。CSSハードコード色値をCSS変数に置換。タイムラインに「完了タスクを非表示」フィルタを追加。ビュー選択状態を `localStorage` で保持                                                                                                                                                                                            |
| 2026-06-16 | ダッシュボードのサマリーカードを刷新（期間学習時間・進捗率・完了タスク・読了書籍の4枚構成）。学習時間表示を分→時間単位に変更。テーブルビューに「+」ボタン追加。カレンダー・タイムラインに「+ 子タスク追加」ボタン追加。テーブルグループ行の折りたたみ状態をlocalStorageで保持（v2.2.0）                                                                                                                                                 |
| 2026-06-17 | カンバン完了列の表示条件を変更：全子タスクが完了かつ `end_date` が当年のもののみ完了列に表示。`calcStatus()` に `completedThisYear` 判定を追加（v2.3.0）                                                                                                                                                                                                                                                                                |
| 2026-06-17 | 設計レビュー（Gemini）の指摘を反映しドキュメントを整備（v2.4.0）：(1) データ構造のID型を `INT UNSIGNED`（number）に統一・日付フィールドの推奨型を `DATE` に明記。(2) 親タスク削除時の制約を明記。(3) 学習時間の単位変換ルールを追記。(4) ステップの即時保存の性質を明記。(5) 書籍進捗率の100%上限クリップを明記。(6) タイムライン完了タスク非表示フィルタの挙動を明記。                                                                 |
| 2026-06-18 | マンダラチャート機能を追加（v2.5.0）：新画面「⑥ マンダラチャート」を追加。9×9グリッド・達成トグル・進捗バー・複数マンダラのDB保存（mandalas / mandala_cells テーブル）。                                                                                                                                                                                                                                                                |
| 2026-06-18 | マンダラチャートにセル×子タスク紐づけ機能を追加（v2.6.0）：`mandala_cell_tasks` 中間テーブルを追加（多対多）。「＋タスク」ボタンクリック→選択モーダル→即時API保存のフロー。セル内にタスクタイトル＋ステータスドットを表示。タスクタイトルクリックで編集モーダルを開く。紐づき解除は「×」ボタンで即時API通信。マンダラ取得APIを拡張し各セルに `tasks` 配列を含める。新規APIエンドポイント3本（セル紐づきタスク取得・追加・解除）を追加。 |
